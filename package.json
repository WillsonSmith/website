{
  "name": "w-2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "wireit",
    "watch": "wireit",
    "build": "wireit",
    "build:ssg": "wireit"
  },
  "wireit": {
    "dev": {
      "command": "concurrently \"npm run watch\" \"wds\""
    },
    "watch": {
      "command": "concurrently \"tsc -w\" \"node ssg.config.js\""
    },
    "build": {
      "command": "tsc"
    },
    "build:ssg": {
      "command": "npm run build -w ssg"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/ssg"
  ],
  "devDependencies": {
    "@open-wc/dev-server-hmr": "^0.2.0",
    "@web/dev-server": "^0.2.5",
    "concurrently": "^8.2.0",
    "prettier": "^3.0.0",
    "ssg": "^1.0.0",
    "typescript": "^5.1.6",
    "wireit": "^0.9.5"
  },
  "dependencies": {
    "@webcomponents/template-shadowroot": "^0.2.1",
    "lit": "^3.0.0-pre.0",
    "tslib": "^2.6.0"
  }
}
